import React from 'react';
import { Box, Text } from 'ink';

export const App = (): React.JSX.Element => {
  return (
    <Box>
      <Text color="green">NodePi Initialization...</Text>
    </Box>
  );
};
